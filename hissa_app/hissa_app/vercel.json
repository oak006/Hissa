{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "git clone --depth 1 --branch stable https://github.com/flutter/flutter.git .flutter && .flutter/bin/flutter config --enable-web && .flutter/bin/flutter precache --web && .flutter/bin/flutter pub get",
  "buildCommand": ".flutter/bin/flutter build web --release",
  "outputDirectory": "build/web"
}
